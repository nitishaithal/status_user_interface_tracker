<div class ="field">
	
</div>
<div class = "printing">
	<h1>Projects</h1>
	<% @post = Post.find_by_user_id(current_user.id) %>
<% @posts.each do |post| %>
	 <u><%= link_to post.title, post %></u>
	 	<p> published 
		<%= time_ago_in_words(post.created_at) unless post.created_at.blank? %> ago
	</p>
<% end %>
</div>
<br/>